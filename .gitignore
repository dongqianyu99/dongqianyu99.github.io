# ===================================================================
# Hugo Specific
# ===================================================================

# Hugo 生成的最终网站，这是最重要的忽略项。
# 这个目录应该在服务器上由源码构建，而不是直接上传。
/public/

# Hugo 处理和缓存资源的地方 (Sass, PostCSS, Minified JS, Resized Images)。
# 这个目录也可以在构建时重新生成。
/resources/

# 很多现代主题使用 Pagefind 作为客户端搜索工具，
# 这是 Pagefind 生成的搜索索引，属于构建产物。
/static/pagefind/


# ===================================================================
# Node.js Dependencies (如果你的主题或项目使用npm/yarn/pnpm)
# =================================G==================================

# 包含所有下载的依赖包，体积巨大，不应上传。
# 可以通过 `npm install` 或 `yarn install` 重新安装。
/node_modules/


# ===================================================================
# Operating System & System Files
# ===================================================================

# macOS 系统自动生成的文件
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Windows 系统自动生成的文件
Thumbs.db
ehthumbs.db


# ===================================================================
# Editor & IDE Directories
# ===================================================================

# 这些目录存储用户个人的编辑器设置，不应在团队间共享。
.vscode/
.idea/
*.sublime-project
*.sublime-workspace


# ===================================================================
# Secrets & Environment Variables
# ===================================================================

# 关键安全项：绝不要上传包含密码、API 密钥等敏感信息的文件！
.env
.env.*
# 但允许上传示例文件，这是最佳实践。
!/.env.example
!/.env.sample


# ===================================================================
# Log files
# ===================================================================

# 调试过程中生成的日志文件。
npm-debug.log*
yarn-error.log
yarn-debug.log


# ===================================================================
# Other Temporary & Backup Files
# ===================================================================

*.bak
*.tmp
*.swp

.hugo_build.lock